tmp/
.DS_Store
.env
.env.*
scripts/subscribers.json
node_modules/
package-lock.json
scripts/instagram/out/
scripts/cartoon/out/
scripts/promo/out/

# 2026-05-14 sandbox proxy가 Supabase 차단이라 데이터 파일도 git에 두고
# 작업 공간으로 사용. GitHub Actions runner(db-sync.yml)가 push 받으면 자동 DB sync.

# Claude Code 로컬 권한 설정 (사용자별·머신별로 달라 공유 X)
.claude/settings.local.json
.claude/*.lock

# Supabase CLI
supabase/.branches
supabase/.temp
reference/

# /stocks 한글명 번역 캐시 (로컬 재생성용, 재현 가능)
stocks/.ko-cache.json

# 친구톡 프롬프트 실험용 로컬 스크래치 (커밋·push 안 함 — 프로덕션 격리)
scratch/
