# Top7 — 유지보수 컨텍스트

미국·한국·일본 주식 + AI 기업 + 글로벌 ETF + 원자재·매크로 + 유니콘을 7개 탭으로 보여주는 통합 투자 대시보드.

- 파일: `index.html`(렌더링 UI) / `data/`(데이터 14개 파일 + version.js) / `README.md`
- 데이터 파일은 로컬 상대경로로 fetch (`./data/*.js`)

## 파일 구조

```
ai-investment/
├── index.html                  ← 통합 UI (7개 탭)
├── CLAUDE.md                   ← 이 파일
├── data/
│   ├── version.js              ← 데이터 버전 (탭 전환 시 자동 체크)
│   ├── stocks-data.js          ← 미국 주식 데이터
│   ├── stocks-update.js        ← 미국 주식 변경 이력
│   ├── kr-stocks-data.js       ← 한국 주식 데이터
│   ├── kr-stocks-update.js     ← 한국 주식 변경 이력
│   ├── jp-stocks-data.js       ← 일본 주식 데이터
│   ├── jp-stocks-update.js     ← 일본 주식 변경 이력
│   ├── ai-data.js              ← AI 기업 데이터
│   ├── ai-update.js            ← AI 변경 이력
│   ├── etf-data.js             ← 글로벌 ETF 데이터
│   ├── etf-update.js           ← ETF 변경 이력
│   ├── commodity-data.js       ← 원자재·매크로 데이터
│   ├── commodity-update.js     ← 원자재 변경 이력
│   ├── unicorn-data.js         ← 유니콘·프리IPO 데이터
│   └── unicorn-update.js       ← 유니콘 변경 이력
└── README.md
```

---

## 미국 주식 (stocks)

대상 파일: `data/stocks-data.js`, `data/stocks-update.js`

### 문서 성격 (절대 어기지 말 것)
- **AI 슈퍼사이클 문서가 아니다.** 섹터가 먼저, 테마는 나중.
- 18개 카테고리가 동등한 비중으로 보여야 한다.
- AI 사이클 플로우는 "관점 중 하나"로만 언급되며 메인이 아니다.

### 현재 상태
- 18 카테고리 × 정확히 7종목 = 108종목
- 재무단위 $B, FY25 추정 / FY26E 컨센서스, 적자는 음수 표기

### 18개 카테고리
AI 플랫폼 · 반도체 · 데이터센터 · 냉각 시스템 · 물·수자원 · 전력·그리드 ·
원자력·SMR · 바이오·헬스케어 · 에너지 · 친환경·청정기술 · 자동차·모빌리티 ·
항공우주·방산 · 양자·크립토 · 사이버보안 · 핀테크·결제 · 금융·은행 ·
로봇·자동화 · 소비·리테일

### data/stocks-data.js 구조
```js
const data = [{
  title: "카테고리명",
  tag: "카테고리 설명 (1줄)",
  stocks: [
    { tk:"TICKER", nm:"기업명", rs:"한 줄 이유", r1:"전년매출", p1:"전년순익", r2:"올해매출E", p2:"올해순익E" }
  ]
}]
```
- 각 카테고리 stocks 배열은 **정확히 7개** 유지
- 숫자는 문자열로 (소수점 표기 자유도 확보). 적자는 `"-0.10"` 식 음수 문자열

### data/stocks-update.js 구조
```js
const updates = [{
  date: "2026-04-13 01:30 KST",   // 한국 시간(GMT+9) 기준, 시·분 포함
  summary: "요약 한 줄",
  changes: [{ type, sector, detail }]
}]
```
- **version 필드 없음** — 버전 번호를 쓰지 않는다.

### 편집 규칙
- **한 줄 이유(rs)**: 12단어 이내. 기술 스펙 + 시장 맥락 함께. 단순 나열 금지.
- **재무 수치**: 컨센서스 기반 추정치. 상대적 규모감이 목적이며 소수점 둘째 자리 다투지 않음.
- **대형주**: 정수 반올림 (예: `"270"`). **소형주**: 소수점 (예: `"2.6"`).
- **적자 기업**: NM/NA 쓰지 않고 음수 표기.

### 제외 규칙 (해당 시 즉시 교체)
- 회계·거버넌스 이슈 (예: SMCI)
- 상용화 지연 (예: NuScale SMR)
- 재무 악화 (예: POWL)
- ETF·펀드·레버리지·인버스 일체

### 새 종목 추가 체크리스트 (3개 이상 Yes여야 추가)
1. 이 기업을 빼고 해당 섹터를 설명할 수 있는가? (No 여야 함)
2. FY26E 컨센서스가 존재하는가?
3. 12단어 이내로 차별화 포인트를 쓸 수 있는가?
4. 기존 6개 중 하나보다 대표성이 높은가?

### 카테고리 변경 원칙
- 새 카테고리 추가 시 **기존 카테고리 하나를 먼저 통폐합**해 자리를 만든다.
- 추가보다 교체가 원칙. 카테고리 수는 원칙적으로 18개 유지.

### 의식적으로 하지 않는 것
- 목표가 / 매수매도 의견 표시
- 시가총액·PER 등 밸류에이션 지표
- 차트·주가 히스토리
- ETF 추천 / 미국 외 개별주 확장

### 주식 업데이트 체크리스트 (매 요청 시 이 순서로)
1. 웹검색으로 오늘 날짜 기준 최신 컨센서스·이벤트 확인 (FY 실적 확정, 대형 M&A, 회계이슈 등)
2. `data/stocks-data.js` 수정 — 변경된 종목만 최소 diff로
3. 회계/거버넌스 이슈 발생 기업은 즉시 제외하고 동일 섹터 대체주로 교체
4. **이력 기록 (두 파일 역할 구분)**
   - `data/stocks-update.js` — 종목 교체·수치 갱신·제외 사유 등 **데이터 변경 이력**을 날짜+시간(KST)별로 추가. 일반 사용자가 "무엇이 왜 바뀌었는지" 파악할 수 있는 전문적 톤으로 작성. **version 필드 없음.**
   - `README.md` Changelog — 파일 구조·렌더링·빌드 등 **기술적 변경**만 기록. 데이터 변경은 적지 않는다.
5. `data/version.js`의 `DATA_VERSION` 값을 현재 KST 시간으로 갱신
6. 변경 사항 요약을 채팅으로 보고 (커밋은 사용자가 직접)

### 부분 업데이트 지원
사용자가 특정 섹터/종목만 지정하면 나머지는 건드리지 않는다.
예: "반도체 카테고리만 최신 실적으로 갱신해줘" → `data/stocks-data.js`의 해당 배열만 수정.

---

## 한국 주식 (kr)

대상 파일: `data/kr-stocks-data.js`, `data/kr-stocks-update.js`

### 문서 성격 (절대 어기지 말 것)
- 미국 주식과 동일한 원칙: **섹터가 먼저, 테마는 나중.**
- 18개 카테고리가 동등한 비중으로 보여야 한다.

### 현재 상태
- 18 카테고리 × 정확히 7종목 = 108종목
- 재무단위 천억원, FY25 추정 / FY26E 컨센서스, 적자는 음수 표기

### 18개 카테고리
AI·소프트웨어 · 반도체 · 2차전지 · 바이오·제약 · 자동차·부품 · 방산 ·
조선·해양 · 금융 · 전력·에너지 · 화학·소재 · 게임 ·
엔터·콘텐츠 · 화장품·뷰티 · 유통·소비 · 식품·음료 · IT·전자 ·
통신·인터넷 · 건설·플랜트

### data/kr-stocks-data.js 구조
```js
const data = [{
  title: "카테고리명",
  tag: "카테고리 설명 (1줄)",
  stocks: [
    { tk:"종목코드", nm:"기업명", rs:"한 줄 이유", r1:"전년매출", p1:"전년순익", r2:"올해매출E", p2:"올해순익E" }
  ]
}]
```
- 각 카테고리 stocks 배열은 **정확히 7개** 유지
- tk는 6자리 종목코드 (예: `"005930"`)
- 숫자는 문자열, 단위는 천억원. 적자는 `"-0.15"` 식 음수 문자열

### data/kr-stocks-update.js 구조
```js
const updates = [{
  date: "2026-04-13 09:00 KST",   // 한국 시간(GMT+9) 기준, 시·분 포함
  summary: "요약 한 줄",
  changes: [{ type, sector, detail }]
}]
```
- **version 필드 없음** — 버전 번호를 쓰지 않는다.

### 편집 규칙
- **한 줄 이유(rs)**: 12단어 이내. 기술 스펙 + 시장 맥락 함께. 단순 나열 금지.
- **재무 수치**: 컨센서스 기반 추정치. 상대적 규모감이 목적이며 소수점 둘째 자리 다투지 않음.
- 천억원 단위 표기. **대형주**: 정수 (예: `"1680"`). **소형주**: 소수점 (예: `"4.5"`).
- **적자 기업**: NM/NA 쓰지 않고 음수 표기 (예: `"-1.5"`).

### 제외 규칙 (해당 시 즉시 교체)
- 회계·거버넌스 이슈
- 상용화 지연 또는 실질 매출 없는 기업
- 재무 악화 (지속적 적자 심화, 자본잠식 등)
- ETF·펀드·레버리지·인버스 일체
- 비상장 기업, 해외 상장 기업 (예: 쿠팡 CPNG)

### 새 종목 추가 체크리스트 (3개 이상 Yes여야 추가)
1. 이 기업을 빼고 해당 섹터를 설명할 수 있는가? (No 여야 함)
2. FY26E 컨센서스가 존재하는가?
3. 12단어 이내로 차별화 포인트를 쓸 수 있는가?
4. 기존 6개 중 하나보다 대표성이 높은가?

### 카테고리 변경 원칙
- 새 카테고리 추가 시 **기존 카테고리 하나를 먼저 통폐합**해 자리를 만든다.
- 추가보다 교체가 원칙. 카테고리 수는 원칙적으로 18개 유지.

### 의식적으로 하지 않는 것
- 목표가 / 매수매도 의견 표시
- 시가총액·PER 등 밸류에이션 지표
- 차트·주가 히스토리
- ETF 추천

### 한국 주식 업데이트 체크리스트 (매 요청 시 이 순서로)
1. 웹검색으로 오늘 날짜 기준 최신 컨센서스·이벤트 확인 (FY 실적 확정, 대형 M&A, 회계이슈 등)
2. `data/kr-stocks-data.js` 수정 — 변경된 종목만 최소 diff로
3. 회계/거버넌스 이슈 발생 기업은 즉시 제외하고 동일 섹터 대체주로 교체
4. **이력 기록 (두 파일 역할 구분)**
   - `data/kr-stocks-update.js` — 종목 교체·수치 갱신·제외 사유 등 **데이터 변경 이력**을 날짜+시간(KST)별로 추가. 일반 사용자가 "무엇이 왜 바뀌었는지" 파악할 수 있는 전문적 톤으로 작성. **version 필드 없음.**
   - `README.md` Changelog — 파일 구조·렌더링·빌드 등 **기술적 변경**만 기록. 데이터 변경은 적지 않는다.
5. `data/version.js`의 `DATA_VERSION` 값을 현재 KST 시간으로 갱신
6. 변경 사항 요약을 채팅으로 보고 (커밋은 사용자가 직접)

### 부분 업데이트 지원
사용자가 특정 섹터/종목만 지정하면 나머지는 건드리지 않는다.
예: "반도체 카테고리만 최신 실적으로 갱신해줘" → `data/kr-stocks-data.js`의 해당 배열만 수정.

---

## AI 기업 (ai)

대상 파일: `data/ai-data.js`, `data/ai-update.js`

### 선정 원칙
"빅테크"가 아닌 AI 생태계 실질 플레이어 기준:
프론티어 모델 / 생태계 영향력 / 인프라 지배력 / 오픈소스 영향 / 시장 규모
→ IBM·Oracle보다 Anthropic·xAI·Mistral 우선.

### 현재 수록 기업 (10)
OpenAI, Anthropic, Google DeepMind, xAI, Meta AI,
Microsoft, NVIDIA, Amazon AWS, Apple, Mistral AI

### 배지 체계 (BL 객체)
llm / img / vid / agt / code / ui / hw / sci

### data/ai-data.js 구조
```js
const META = { year, month };
const BL = { llm, img, vid, agt, hw, code, sci, ui };
const data = [{
  rank, name, url, type, focus,
  products: [{ name, url, isNew, badges:[], desc }]
}]
```

### data/ai-update.js 구조
```js
const UPDATES = [{
  date: "2026-04-13 00:40 KST",   // 한국 시간(GMT+9) 기준, 시·분 포함
  entries: ["변경 내용 문자열", ...]
}]
```

### 작성 규칙
- isNew: 이번 업데이트 신규 항목만 true. 출시 4~6주 경과 시 false로 내림.
- desc: 기술 스펙 + 시장 맥락 함께. 단순 스펙 나열 금지.
- `data/ai-update.js`: 콘텐츠 변경(모델·제품·펀딩·인수 등)은 여기에 날짜+시간(KST)별 기록.
- `README.md` 버전 히스토리: 소스코드 구조·파일 트리·UI 로직 등 기술적 변경만 기록.

### AI 업데이트 체크리스트 (매 요청 시 이 순서로)
1. 웹검색으로 오늘 날짜 기준 각 기업의 최신 모델/제품/펀딩/인수 확인
2. `data/ai-data.js` 수정 — 변경된 기업만 최소 diff로
3. 이전 NEW 배지들을 isNew:false 로 내리기
4. 새 항목에 isNew:true 부여
5. `data/ai-update.js`에 콘텐츠 변경 사항 기록 (제품·모델·펀딩·인수 등, 전문적 톤). date에 한국 시간(KST) 포함.
6. `README.md` 버전 히스토리 — 기술적 변경이 있을 때만 추가
7. `data/version.js`의 `DATA_VERSION` 값을 현재 KST 시간으로 갱신
8. 변경 사항 요약을 채팅으로 보고 (커밋은 사용자가 직접)

---

## 일본 주식 (jp)

대상 파일: `data/jp-stocks-data.js`, `data/jp-stocks-update.js`

### 현재 상태
- 10 카테고리 × 정확히 7종목 = 70종목
- 재무단위 천억엔, FY25 실적(3월 결산) / FY26E 컨센서스, 적자는 음수 표기
- 도쿄증권거래소 프라임 상장 종목 대상

### 10개 카테고리
반도체 장비 · 자동차 · 전자·IT · 금융 · 로봇·자동화 · 방산·중공업 · 소비·리테일 · 게임·엔터 · 제약·헬스케어 · 소재·화학

### data/jp-stocks-data.js 구조
```js
const data = [{
  title: "카테고리명",
  tag: "카테고리 설명 (1줄)",
  stocks: [
    { tk:"4자리코드", nm:"기업명", rs:"한 줄 이유", r1:"전년매출", p1:"전년순익", r2:"올해매출E", p2:"올해순익E" }
  ]
}]
```

### 일본 주식 업데이트 체크리스트 (매 요청 시 이 순서로)
1. 웹검색으로 오늘 날짜 기준 최신 컨센서스·이벤트 확인
2. `data/jp-stocks-data.js` 수정 — 변경된 종목만 최소 diff로
3. 회계/거버넌스 이슈 발생 기업은 즉시 제외하고 동일 섹터 대체주로 교체
4. `data/jp-stocks-update.js`에 데이터 변경 이력 추가 (날짜+시간 KST)
5. `data/version.js`의 `DATA_VERSION` 값을 현재 KST 시간으로 갱신
6. 변경 사항 요약을 채팅으로 보고 (커밋은 사용자가 직접)

---

## 글로벌 ETF (etf)

대상 파일: `data/etf-data.js`, `data/etf-update.js`

### 현재 상태
- 8 카테고리 × 5 ETF = 40 ETF
- 필드: 운용규모($B), 보수(%), YTD 수익률, 1Y 수익률

### 8개 카테고리
AI·반도체 · 방산·보안 · 에너지·원자력 · 클린에너지 · 크립토·블록체인 · 미국 대형주 · 배당·인컴 · 신흥국·글로벌

### data/etf-data.js 구조
```js
const data = [{
  title: "카테고리명",
  tag: "카테고리 설명",
  stocks: [
    { tk:"TICKER", nm:"ETF명", rs:"한 줄 설명", aum:"운용규모$B", er:"보수%", ytd:"YTD수익률", y1:"1Y수익률" }
  ]
}]
```

### 글로벌 ETF 업데이트 체크리스트 (매 요청 시 이 순서로)
1. 웹검색으로 각 ETF의 최신 AUM·수익률·보수 확인
2. `data/etf-data.js` 수정 — 변경된 ETF만 최소 diff로
3. 상장폐지·합병된 ETF는 즉시 교체
4. `data/etf-update.js`에 데이터 변경 이력 추가 (날짜+시간 KST)
5. `data/version.js`의 `DATA_VERSION` 값을 현재 KST 시간으로 갱신
6. 변경 사항 요약을 채팅으로 보고 (커밋은 사용자가 직접)

---

## 원자재·매크로 (commodity)

대상 파일: `data/commodity-data.js`, `data/commodity-update.js`

### 현재 상태
- 6 카테고리 × 4 항목 = 24 항목
- 필드: 현재가, YTD 변동, 1Y 변동, 52주 범위

### 6개 카테고리
귀금속 · 에너지 · 산업금속 · 배터리 소재 · 농산물 · 매크로 지표

### data/commodity-data.js 구조
```js
const data = [{
  title: "카테고리명",
  tag: "카테고리 설명",
  stocks: [
    { tk:"심볼", nm:"원자재명", rs:"한 줄 설명", price:"현재가", ytd:"YTD", y1:"1Y", range:"52주 범위" }
  ]
}]
```

### 원자재·매크로 업데이트 체크리스트 (매 요청 시 이 순서로)
1. 웹검색으로 최신 선물 가격·매크로 지표 확인
2. `data/commodity-data.js` 수정 — 변경된 항목만 최소 diff로
3. `data/commodity-update.js`에 데이터 변경 이력 추가 (날짜+시간 KST)
4. `data/version.js`의 `DATA_VERSION` 값을 현재 KST 시간으로 갱신
5. 변경 사항 요약을 채팅으로 보고 (커밋은 사용자가 직접)

---

## 유니콘·프리IPO (unicorn)

대상 파일: `data/unicorn-data.js`, `data/unicorn-update.js`

### 현재 상태
- 6 카테고리 × 5 기업 = 30 기업
- 필드: 밸류에이션, 최근 라운드, 섹터, IPO 전망
- 비상장 기업만 수록 (상장 시 즉시 제외)

### 6개 카테고리
AI·ML · 핀테크 · 엔터프라이즈 SW · 우주·모빌리티 · 바이오·헬스 · 크립토·Web3

### data/unicorn-data.js 구조
```js
const data = [{
  title: "카테고리명",
  tag: "카테고리 설명",
  stocks: [
    { nm:"기업명", rs:"한 줄 설명", val:"밸류에이션", round:"최근 라운드", sector:"섹터", ipo:"IPO 전망" }
  ]
}]
```

### 유니콘 업데이트 체크리스트 (매 요청 시 이 순서로)
1. 웹검색으로 각 기업의 최신 펀딩/밸류에이션/IPO 동향 확인
2. `data/unicorn-data.js` 수정 — 변경된 기업만 최소 diff로
3. 상장 완료된 기업은 즉시 제외하고 동일 카테고리 대체 기업 편입
4. `data/unicorn-update.js`에 데이터 변경 이력 추가 (날짜+시간 KST)
5. `data/version.js`의 `DATA_VERSION` 값을 현재 KST 시간으로 갱신
6. 변경 사항 요약을 채팅으로 보고 (커밋은 사용자가 직접)

---

## UI·디자인 수정 규칙
- HTML/CSS 등 **디자인 관련 수정 시 반드시 `.claude/DESIGN.md`를 먼저 읽고** 디자인 시스템(색상·타이포·간격·컴포넌트 스펙)을 따른다.
- 대규모 디자인 변경이나 새 컴포넌트 추가 시 `/designer` 커맨드를 활용한다.

## 공통 규칙

### 변경 감지 방법론 (모든 탭 공통, 체크리스트 실행 시 반드시 적용)

"이번 주 어닝·대형 M&A만 반영"처럼 기준을 좁히면 실제로 반영해야 할 변화를 대량 놓친다. 각 탭 체크리스트를 수행할 때 아래 3단계를 함께 돌려라.

**1. rs 필드 유효성 검증**
- 현재 파일의 각 종목 `rs` 문구가 특정 사건을 명시하고 있으면(예: "Q1 $35B 실적", "$1.8B 인수", "신모델 출시"), 그 사건이 **여전히 최신 상태**인지 확인.
- 이미 반영된 이벤트 이후 새 사건이 있거나, 진행 중 상태가 완료된 경우엔 최신 사건으로 교체.
- 예: `IONQ rs:"이온트랩 양자"` → SkyWater 인수(2026-01 발표, Q2~Q3 클로징 예정) 발표 후라면 `rs:"SkyWater $1.8B 인수·풀스택 양자 수직통합"`으로 교체 대상.

**2. 개별 티커·기업 타겟 검색**
- 광역 시장 검색("코스피 종목 실적", "Japan earnings week"처럼)만 하면 개별 종목 변화를 놓치기 쉽다.
- 각 탭에서 **대표 상위 3~5개 티커/기업을 개별 검색**: `TICKER April 2026 news earnings` 또는 `기업명 최신 뉴스 2026년 4월` 형식.
- 주식 탭은 시가총액·영향력 상위 종목부터, AI 탭은 모델 활성도 높은 기업부터 우선.

**3. 진행 중 이벤트 추적**
- "이번 주 이벤트"만 보면 수주~수개월 단위로 진행 중인 변화(인수 클로징 대기, 신제품 출시 예정, IPO 로드쇼, FY26E 가이던스 수정 등)를 놓친다.
- 현재 `rs`에 반영되지 않은 **상태 전환성 이벤트**도 수정 대상.

**4. 외부 서프라이즈 스캔 (폐쇄 루프 방지)**
- 위 1~3단계는 모두 **기존 포함 항목만** 보는 방식이라, 외부에서 대표성이 더 높은 신규 종목·기업이 등장해도 감지 못 한다.
- 각 탭에서 **섹터·테마 단위 광역 뉴스 1~2회 스캔**: `반도체 섹터 주요 뉴스 2026년 4월`, `Q1 2026 earnings surprises`, `bio M&A wave 2026`, `AI 신규 유니콘 펀딩` 등.
- 신호 유형: 대형 IPO 성공, 섹터 재편급 M&A, 파괴적 신제품/기술 출시, 신규 대형 펀딩, 카테고리 리더십 변화 등.
- **교체 의사결정은 기존 규칙 그대로 보수적 적용**: "제외 규칙(회계이슈·재무악화 등)"이나 각 탭의 "새 종목 추가 체크리스트(3개 이상 Yes)"를 만족할 때만 실제 스왑. 확신 부족 시 edit 생략하고 관찰.

**원칙: 조사는 광범위, 편집은 보수적**
- 각 탭 체크리스트의 "X% 이상 변동" 수치는 참고치일 뿐, 절대 문턱이 아니다. **방향성이 의미 있는 변화**면 반영을 고려해라.
- "애매하면 건드리지 않는다"는 편집 판단의 원칙이지, **조사 범위를 좁히는 구실이 아니다.** 기존 종목만 점검하는 폐쇄 루프를 경계해라.

### 데이터 버전 갱신
- 데이터 파일(data/*.js)을 수정할 때마다 `data/version.js`의 `DATA_VERSION` 값을 갱신한다.
- 형식: `"YYYYMMDD-HHmm"` (예: `"20260413-1005"`). 시간은 KST 기준.
- 사용자가 사이트에 접속 중이면, 탭 전환 시 version.js를 체크하여 버전이 다르면 자동으로 최신 데이터를 다시 로드한다.

### 업데이트 시간 표기
- date 필드에 **한국 시간(GMT+9)** 기준 날짜+시간을 기록: `"2026-04-13 01:30 KST"`
- **시간은 반드시 시스템 시간(`date` 명령)으로 확인한 뒤 기록한다. 추측하지 말 것.**
- 과거 기록 중 시간이 없는 항목은 그대로 둔다 (소급 수정 안 함).

### 변경 로그 이원화 원칙
- **update.js 파일들** — 일반 사용자 대상. 어떤 기업/종목의 무엇이 왜 변경되었는지를 전문적이고 읽기 쉬운 문체로 기록. 기술적 세부사항 제외.
- **README.md** — 개발자/기여자 대상. 소스코드 구조 변경, 파일 추가/삭제, UI 로직 수정 등 기술적 변경만 기록.

### update.js 동일 날짜 이력 누적·통합 원칙
모든 `data/*-update.js`(stocks·kr-stocks·jp-stocks·ai·etf·commodity·unicorn)에 공통 적용.

**스키마 (공통 확장 필드)**
```js
{
  date: "YYYY-MM-DD HH:MM KST",   // 엔트리 생성 시각 (최신 이벤트 시각)
  badge: "통합" | "개별",          // 엔트리 성격
  summary: "요약 한 줄",            // (통합 엔트리는 재작성, 개별은 단일 이벤트 요약)
  changes: [                       // AI 탭은 entries[] 사용
    { type, sector, detail, time: "YYYY-MM-DD HH:MM KST" }
    // time은 각 변경 이벤트의 실제 발생 시각
  ]
}
```
AI 탭은 `entries: [{ text, time }]` 형식. 레거시 string 엔트리도 호환.

**누적·통합 흐름 (테스트 단계, badge "개별"은 추후 제거 예정)**
1. 새 변경 발생 시 **항상 `badge:"개별"` 엔트리로 prepend**. 이 엔트리의 `changes[].time`은 해당 이벤트 시각.
2. 같은 날 기존 엔트리 중 **동일 종목** 또는 **같은 섹터·카테고리**로 묶일 수 있는 개별 엔트리가 있으면 → 새 `badge:"통합"` 엔트리를 맨 앞에 추가. 기존 `badge:"개별"` 엔트리들은 **그대로 유지**(삭제 금지).
3. "통합" 엔트리의 `changes[]`는 해당 개별 엔트리들의 변경 사항을 합치고 중복 제거, 각 change의 `time`은 원본 이벤트 시각 유지. `summary`는 통합 관점으로 재작성. 엔트리 `date`는 통합 생성 시각.
4. 결과 배열 모습: `[통합(최상단)] → [개별 N] → ... → [개별 1] → [전일 엔트리]` (같은 날 범위 내, 역시간순).
5. **다른 날짜 엔트리는 소급 수정 금지.**
6. 통합 판단이 애매하면 누적(개별 prepend)만 하고 통합 생략.

**제외·추가 사유 기록 원칙**
종목·기업 교체(제외 규칙 적용, 새 종목 추가)가 발생하면 `detail`에 **사유를 자연어로 명시**한다.
- 예: `"제외: SMCI(회계이슈·SEC 조사 지속) → 추가: NVDA(동일 섹터 대표성 우위, FY26E 컨센서스 충실)"`
- 예: `"알테오젠 Q1 확정 실적 반영 + 대형 제약 M&A로 ABCD 추가(바이오 카테고리 리더십 이동)"`
- 단순 수치 갱신은 사유 생략 가능.

### 마지막 업데이트 기록
- 2026-04-13 10:05 KST (stocks): TSMC Q1 $35.6B 사상 최대 실적 반영
- 2026-04-13 10:05 KST (kr): 반도체 FY26E 대폭 상향(삼성 500/140조, SK하이닉스 170/80조), 한화에어로 연결 기준 보정, 알테오젠 확정 실적
- 2026-04-13 10:05 KST (ai): OpenAI DOD 논란·#QuitGPT, 대중국 증류 공동 대응, xAI 구조조정
